# Скопируйте в Desktop/.env — приложение загрузит его автоматически
# На Windows с Docker/WSL используйте 127.0.0.1 — localhost может уйти в wslrelay (HTTP 503).
PROXY_ACCESS_API_URL=http://127.0.0.1:8000
